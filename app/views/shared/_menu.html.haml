- if user_signed_in? 
  %ul
    - if can? :read, User
      %li= link_to 'Users', users_path
    - if can? :read, Role
      %li= link_to 'Roles', roles_path
    - if can? :read, Site
      %li= link_to 'Sites', sites_path